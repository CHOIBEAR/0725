
# 📗 파이썬 수업 복습 퀴즈 🐍

# ❓ Q1.
# 함수를 정의할 때 사용하는 키워드는 무엇인가요?
# 그리고 함수를 실행하려면 무엇을 해야 하나요?

# def 로 정의한다. 실행할땐 함수에 매개변수를 담아 호출한다.

#ex)
def a ():
    print ("실행")
    return
a()
    
# ❓ Q2.
# 다음 중 재귀 함수의 특징은 무엇인가요?

# 1 반복문을 내부에 포함한다

# 2 함수 안에서 자기 자신을 호출한다 정답.

# 3 외부에서만 호출 가능하다

# 4 변수 이름을 반복해서 쓴다

# ❓ Q3.
# 클래스 내부 메서드를 정의할 때 첫 번째 매개변수로 반드시 넣어야 하는 예약 이름은 무엇인가요?
# self/s

# ❓ Q4.
# 전역변수와 지역변수의 차이를 간단히 설명해보세요.
# (힌트: 어디에서 정의되고 어디까지 사용 가능한가요?)
# 전역 변수는 생성된 파일 내부 전역에서 사용가능.
# 지역 변수는 생성된 함수 내부에서만 사용가능. 단 리턴으로 받는다면 다른 함수에서도 사용 가능.

# ❓ Q5.
# 클래스 속성 접근을 제한하고, 안전하게 값을 가져오거나 바꾸는 방식 두 가지는 무엇인가요?
# (힌트: @... 데코레이터와 별도 함수)

# 변수에 __(언더바 2개)를 붙이면 외부에서 직접 접근 막힘
# @property로 값을 읽을 수 있음 (getter 역할)
# 값을 바꿀 땐 별도 메서드로 설정 (setter 역할)

#-----------------------------------------풀이-----------------------------------
# 📘 파이썬 수업 퀴즈 정답 요약 🧠

# ━━━━━━━━━━━━━━━━━━━━
# ❓ Q1. 함수 정의 및 실행 방법
# ━━━━━━━━━━━━━━━━━━━━
# ✔️ 함수는 `def` 키워드로 정의함  
# ✔️ 실행하려면 함수 이름과 괄호로 호출함 (`함수명()`)

# 예시:
# def a():
#     print("실행")
#     return

# a()  # 실행

# ━━━━━━━━━━━━━━━━━━━━
# ❓ Q2. 재귀 함수의 특징
# ━━━━━━━━━━━━━━━━━━━━
# ✔️ 정답: 2번  
# → "함수 안에서 자기 자신을 호출한다"

# ━━━━━━━━━━━━━━━━━━━━
# ❓ Q3. 클래스 내부 메서드의 첫 번째 매개변수
# ━━━━━━━━━━━━━━━━━━━━
# ✔️ 정답: `self` 또는 `s`  
# → 클래스 인스턴스 자신을 의미

# ━━━━━━━━━━━━━━━━━━━━
# ❓ Q4. 전역 변수 vs 지역 변수
# ━━━━━━━━━━━━━━━━━━━━
# ✔️ 전역 변수: 파일 전체에서 사용 가능  
# ✔️ 지역 변수: 함수 내부에서만 사용 가능  
# → 단, `return`으로 값을 넘기면 다른 함수에서도 사용 가능

# ━━━━━━━━━━━━━━━━━━━━
# ❓ Q5. 클래스 속성 접근 제한과 제어
# ━━━━━━━━━━━━━━━━━━━━
# ✔️ `__변수명` → 외부 접근 제한 (private처럼 사용)  
# ✔️ `@property` → 값을 읽는 getter  
# ✔️ setter는 별도 메서드로 값을 바꾸도록 구현함

#-------------------------------2차 문제풀이---------------------------------

# 📘 파이썬 응용 복습 퀴즈 2탄 🧠

# ━━━━━━━━━━━━━━━━━━━━
# ❓ Q1.
# 다음 중 지역 변수를 다른 함수에서 사용하려면 어떻게 해야 하나요?

# 1) global로 선언한다  
# 2) 함수 이름을 바꾼다  
# 3) return을 사용해 값을 전달한다  정답
# 4) del로 삭제한 후 복구한다

# ━━━━━━━━━━━━━━━━━━━━
# ❓ Q2.
# 아래 설명이 의미하는 파이썬 기능은 무엇인가요?

# "클래스 외부에서 변수 접근을 제한하고,  
# 값을 읽을 수는 있지만 직접 수정은 막는 방식."

# `__변수명` → 외부 접근 제한 (private처럼 사용)  
# `@property` → 값을 읽는 getter  
# setter는 별도 메서드로 값을 바꾸도록 구현함

# ━━━━━━━━━━━━━━━━━━━━
# ❓ Q3.
# 다음 중 클래스 생성자에 대한 설명으로 올바른 것은?

# 1) 생성자는 클래스 외부에서만 호출된다  
# 2) 생성자는 `__init__`이라는 이름으로 정의한다  정답
# 3) 생성자는 반드시 매개변수를 하나만 가진다  
# 4) 생성자는 변수명을 정의하는 곳이다

# ━━━━━━━━━━━━━━━━━━━━
# ❓ Q4.
# 아래 코드를 간단히 설명하시오.

# def a(b):  
#     if b:  
#         print(b)  
#         del b[0]  
#         a(b)

# (힌트: 어떤 방식으로 동작하는 함수인가요?)
#재귀함수로 함수를 부르면 다시 자신을 불러낸다.

# ━━━━━━━━━━━━━━━━━━━━
# ❓ Q5.
# 클래스 내부에서 `__변수명` 형식으로 변수를 선언하는 이유는 무엇인가요?  
# (간단히 한 줄로 설명해보세요.)
#외부 접근을 제한하기 위해서.